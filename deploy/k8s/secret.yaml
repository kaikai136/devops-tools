apiVersion: v1
kind: Secret
metadata:
  name: devops-tools-secret
  namespace: default
type: Opaque
stringData:
  DJANGO_SECRET_KEY: ""  # 留空将自动生成，但建议设置一个固定值
  # 如需自定义 CSRF 信任来源，取消注释并设置
  # DJANGO_CSRF_TRUSTED_ORIGINS: "https://your-domain.com"