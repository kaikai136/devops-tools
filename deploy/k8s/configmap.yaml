apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-tools-config
  namespace: default
data:
  app.conf: |
    # Kubernetes deployment config (SQLite backend, single-node app).
    TZ=Asia/Shanghai

    DJANGO_SECRET_KEY=bSQdcS0sfvIHsPJehV1cxEkDE9h-rJ8uDBR6CUZJdLRVmWpDgXXcX5yLItLNdnR6__D9l_SbeyhGXDmcGD71FA
    DJANGO_SECRET_KEY_FILE=/app/data/django-secret-key
    DJANGO_DEBUG=0
    DJANGO_ALLOWED_HOSTS=*
    DJANGO_CSRF_TRUSTED_ORIGINS=http://172.16.0.161:30001,http://172.16.0.162:30001,http://172.16.0.163:30001,http://172.16.0.164:30001
    DJANGO_CORS_ALLOW_ALL_ORIGINS=0
    DJANGO_CORS_ALLOWED_ORIGINS=
    DJANGO_MEDIA_ROOT=/app/media
    DJANGO_SERVE_MEDIA_FILES=1

    DATABASE_ENGINE=sqlite
    DJANGO_DB_PATH=/app/data/db.sqlite3

    GUACD_HOST=guacd
    GUACD_PORT=4822

    RDP_RECORDING_ROOT=/app/recordings
    RDP_RECORDING_RETENTION_DAYS=30
    RDP_RECORDING_DEFAULT_ENABLED=0

    SSH_GATEWAY_ENABLED=1
    SSH_GATEWAY_BIND_HOST=0.0.0.0
    SSH_GATEWAY_PORT=2222
    SSH_GATEWAY_PUBLIC_HOST=172.16.0.161
    SSH_GATEWAY_PUBLIC_PORT=30222
    SSH_GATEWAY_HOST_KEY_PATH=/app/data/ssh-gateway-host-key
